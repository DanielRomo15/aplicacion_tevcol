package com.utc.aplicacion_tevcol.repository;

import com.utc.aplicacion_tevcol.entity.TipoBrigada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoBrigadaRepository extends JpaRepository<TipoBrigada, Long> {
}